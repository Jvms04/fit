# WP-002 — Evidence package

- Increment: G0 / INC-P0-01
- Status: Done
- Date: 2026-09-13
- Branch: `wp-002-g0-harness`
- Base: `main@d6d3abfde5329c33fe1d0df52fbc4017f13825c1`
- Commit/PR: [PR #4](https://github.com/Jvms04/fit/pull/4) integrated after explicit human review of head `18a558721772868de1839bec3844aac838528ecf`; merge commit `35d8edd749fb1e1d77bf95ff18503a88834fd938`; pre-merge runs `34766902701` and `34766902705` succeeded

## Authority and objective

Authority: Approved Implementation Plan Baseline 1.1, especially `IMPLEMENTATION_WORK_BREAKDOWN.md`, `IMPLEMENTATION_ROADMAP.md`, `VALIDATION_AND_SPIKE_EXECUTION_PLAN.md`, `QUALITY_AND_TEST_EXECUTION_PLAN.md`, `DEPENDENCY_RISK_AND_CRITICAL_PATH.md`, `PHASE6_DECISION_REGISTER.md`, and `PHASE7_EXECUTION_BRIEF.md`; Stack Baseline 1.0 and AD-BL-001–007/014/016/018–019/021–024/037 where mapped by the protocols.

WP-002 is a disposable G0 harness and readiness block for `SP-007`, `VAL-001`, `VAL-003`, `VAL-004`, `VAL-006`, `VAL-011`, and `VAL-016@P0`. It owns no Product requirement or flow and creates no production scaffold.

## Authorized outputs

- disposable probe under `tests/` only;
- synthetic fixtures and test-first protocol utilities;
- raw and summarized evidence with no invented budget;
- CI for the harness and governance surface;
- explicit blocked/inconclusive states for unavailable prerequisites.

Forbidden roots remain absent: `apps/`, `services/`, `packages/`, `database/`, and `infra/`. WP-003 is not started. There is no Product UI, Figma, production database, migration, sync, backend, or cloud provisioning.

## Current execution

- Exact remote base confirmed and short branch created.
- Baseline repository tree reproduced byte for byte; tree SHA `e771d03031c7353168a26dcd14c260fe05fd1565`.
- WP-001 baseline verification passed: 7 governance tests and repository check.
- Official Android Platform-Tools 37.0.1 installed in scratch; no ADB device or mDNS service observed.
- Android CLI 1.0.16261425 and Gradle 9.3.1 were prepared locally; SDK package/Maven resolution is blocked by the executor's JVM network boundary. The PR workflow provides the reproducible full Android build lane without changing durable external infrastructure.
- Expo 57.0.21 / RN 0.86.3 disposable harness installed and typechecked; Expo public config resolves SDK 57.0.0 and SQLCipher enabled.
- CI run `34397341873` passed on head `2678cd63c25a96f15a24927dfdecb31a82b09651` and produced the first disposable Android APK.
- Human-side physical attempt 001 on the Galaxy S23 was aborted by a reproducible runner defect: `KEYCODE_HOME` produced `UNKNOWN (0)` without `TotalTime`, and the old aggregator rejected the resulting null values. The zero-byte JSON is invalid; only the diagnostic transcript is retained.
- The test-first correction classifies launch eligibility explicitly, never substitutes `WaitTime`, prepares a real warm relaunch with `KEYCODE_BACK`, and makes an ineligible attempt emit valid diagnostic JSON with a nonzero exit status.
- Corrected head `52ba378e434e2d13fe1f07564ce730850391a708` passed governance run `34488068372` and harness/build run `34488068572`; APK artifact `10157046732` was used for physical attempt 002.
- Attempt 002 on the Samsung Galaxy S23 / Android 16 exited zero and produced valid partial initial-characterization evidence: 5/5 cold and 5/5 warm samples were `MEASURED` with the expected launch state and `TotalTime`.
- The 20362-byte UTF-16LE source is retained with SHA-256 `f44b2201e84b6c7cbb5a4c03cfba8e5b9e5efb8195f6bf645c28fb6845526e3a`; its 9958-byte UTF-8/LF canonical representation has SHA-256 `65f0c68ed89f42ca8239793256128627c60a85b3883d068462be8613e28d1387`. Parsed JSON values are equivalent.
- Human-approved S23-only startup/PSS budgets were preregistered before the formal attempts. They were not applied retroactively to the five-sample characterization.
- The formal runner preparation at head `2ddd535bcdb95ac7259f1ce2ab16098255446ee6` was rejected by independent audit before physical use; it produced no S23 sample and remains non-evidence.
- The corrective preparation keeps the initial runner at 3–10 and the formal runner at exactly 30 cold + 30 warm. Before sampling, it requires the unchanged preregistration commit `4f108a56d1b3553836eac65662d449809715ee87`, matches checkout head to CI provenance, matches the local APK to the CI SHA-256/size/package, pulls and hashes the installed base APK, verifies the installed runtime's 1000-row marker, and enforces the S23/Android 16 row.
- Process exit evidence is package/process-scoped through Android `ApplicationExitInfo` history. Java/Kotlin crash, native crash, ANR, signaled exit, initialization failure, and every non-protocol exit are abnormal; only runner-issued `force-stop` entries are expected. Mid-run ADB failure emits a nonzero `ABORTED_DIAGNOSTIC` JSON retaining acquired samples and raw evidence.
- Formal Attempt 001 used authorized head `27b41f66bb44137978cb972a506f3d941f140f3e` and artifact `10179821471`. It completed 30 cold and 30 requested warm operations with no crash, but all warm outputs were `UNKNOWN (0) / NOT_A_LAUNCH_EVENT` because Android redelivered the intent to the still top-most Activity. It is preserved only as an [aborted diagnostic](wp-002/raw/SP007_S23_FORMAL_ATTEMPT_001_DIAGNOSTIC.md): the cold observations are retained, no warm p95 exists, no warm budget was evaluated, and no stack failure or fallback was established.
- The test-first correction keeps `KEYCODE_BACK` and adds an uncounted warm preflight plus bounded polling before every warm launch. A warm launch is requested only after `dumpsys activity activities` proves the probe is not top/resumed and `pidof` proves its process remains alive. A failed precondition aborts with partial evidence and does not invoke `am start -W` for that warm sample.
- Human audit accepted Formal Attempt 002 performance/warm evidence for the S23 line: 30/30 cold, 30/30 warm, cold startup p95 173 ms, warm startup p95 112 ms, cold TOTAL PSS p95 110087 KB, and a `WARM / MEASURED` preflight. Artifact/runtime/provenance were verified. The immutable 7674836-byte raw remains in human custody with SHA-256 `a9ecc61f10aff94a978a253b717cf01c60ad34e5e6ece01f148967a86fcb7041`; only its [minimized evidence record](wp-002/raw/SP007_S23_FORMAL_ATTEMPT_002_MINIMIZED.md) is versioned.
- Formal Attempt 002 left zero-crash evidence unresolved at that checkpoint: the physical multi-field `ApplicationExitInfo` layout was not parsed, and the final bounded history could not cover the whole run. Independent reanalysis found 16 retained protocol force-stops and no abnormal exit among retained records, but that attempt remained `INSUFFICIENT-FOR-GATE`. The subsequent correction parses the physical layout and captures package-scoped exit history after every cold/warm pair, deduplicating stable record identities so a mid-run abnormal exit survives later history eviction.
- Formal Attempt 003 used corrected head `82878218cf432998bd6b9582ca0b3e88873c68e0`, artifact `10269505251`, APK SHA-256 `5fd2ca6ebd7c1f75c4e662b1a66d48188bf039cad4b93bed23d66f219e354b69`, and unchanged preregistration `4f108a56d1b3553836eac65662d449809715ee87`. The 8963470-byte raw remains under human custody with SHA-256 `8dd6717c1513434d4a4af0f1553a71e6bd7c897510947c81c9dfd25ade71c216`; only its [minimized evidence record](wp-002/raw/SP007_S23_FORMAL_ATTEMPT_003_MINIMIZED.md) is versioned.
- Human audit accepted the Attempt 003 S23 parcel: 30/30 cold and 30/30 warm, cold startup p95 209 ms, warm startup p95 131 ms, cold TOTAL PSS p95 107711 KB, 31 crash checkpoints, 29 unique expected protocol force-stops, zero abnormal exits, zero observed crashes, and `runnerCriteriaMet=true`. This resolves the Attempt 002 zero-crash blocker for the S23 line only; it does not complete SP-007 or promote canonical VAL-001.
- The authorized Node 24-only `VAL-006` parcel ran in a disposable harness under `tests/`. The locked `moment-timezone` 0.6.3 full TZDB 2026c bundle is 715527 bytes with SHA-256 `43f7878a298740ff6acabb9c726c7e5431a94bdca79abad274a6fe6e355bfe81`; the derived canonical `rule_base_id` includes that hash.
- Node `v24.19.0` matched 5/5 literal gap/overlap/half-hour/date-rollover vectors, reproduced the same serialized results offline, detected a different rule-base hash in the handshake, and preserved consolidated/past records while changing only future unconsolidated material under a separately hashed synthetic future-rule asset. The [machine report](wp-002/raw/VAL006_NODE24_PARTIAL.json) is 6699 bytes with SHA-256 `571cc728af288dcc6e220f5f3ff793a70bdee799a7ed0972e9a31ce796dd4e86`.
- This moves only the operational `VAL-006` protocol state to `PARTIAL`. The Hermes Android gate is approved, but runtime remains `NOT-EXECUTED` because this executor has no connected ADB device or discovered mDNS service; Hermes iOS remains `BLOCKED`, the canonical validation is not promoted, and the FormatJS fallback remains inactive.
- The Hermes Android release build embeds the same corpus and exact `2026c` bytes, rejects a different `rule_base_id` before comparison, and emits a field-by-field candidate only when `HermesInternal` is proven at runtime. CI provenance verifies the embedded bytes, the Hermes bytecode signature, and an arm64 React Native runtime library (including the merged `libreactnative.so` layout used by this stack); it does not substitute for physical runtime evidence.
- The dedicated Hermes capture runner was added test-first. It must match checkout head → CI metadata → local APK → installed `base.apk` → package/device row → process-scoped runtime marker before comparing all five Hermes vectors byte-for-byte with the accepted Node result. Any missing link returns diagnostic-invalid and cannot promote VAL-006.
- The controlled local OS-TZDB divergence scenario detected the deliberately different synthetic OS oracle and retained the hashed IANA 2026c bundle as authority. Its 1972-byte report has SHA-256 `08678e357ae903c6e188bd71945fd6768138540806260bb50a02306a9a36b6ab`; the 417-byte fixture has SHA-256 `44a29708e16ef3f33be69e40f61a19dfd816158569ce26ccc142f3c290d59058`. The host OS TZDB was not mutated, so this remains partial evidence.
- VAL-003/004 static readiness was executed against the locked harness. SQLCipher build configuration, SecureStore, 32-byte Expo Crypto CSPRNG use, absence of `Math.random`, device-only accessibility request, and disabled Android backup were verified. The 1170-byte report has SHA-256 `98c64c906b43377dfe6f55a06894e1d80558c76917940e8737b666d03607472d`. No encrypted-file, wrong-key, rekey/crash, A/B lifecycle, biometry, or restore behavior is inferred.
- The human-authorized Android execution at exact head `5b008e9b6782994359d399f5998c9ffd1bb2234e` is accepted as a valid `PARTIAL` parcel for VAL-003/004. The 24327-byte raw report remains in human custody with SHA-256 `3B9A722C56F609707BD6B6DB624700E539B047B9320A6900A4CB85C9E3AFCA7A`; the APK SHA-256 is `07412e3891090700f77b1f52a0cd1325c1243a1edda180c731c86240d33a06b6`. It measured the nine listed Android operations, while `rekey-interruption`, `screen-lock-sealed-state`, and `biometric-change` remain `INCONCLUSIVE`. Account isolation is `NOT_EVALUATED_WITHOUT_AUTH_A_B`; canonical promotion, formal validation, automatic promotion, and fallback remain false.
- Host readiness reconfirmed Node `24.19.0`, Android Platform-Tools `37.0.1-15733141`, zero connected/discovered ADB targets, and absence of Docker, Podman, `psql`, Apple tooling, cloud credentials, and two synthetic Auth accounts. The 1514-byte host report has SHA-256 `413ecd8f5a90e14f474f32d81bec3faa11d15f9795d10a3de2d25ba194a0bf2b`.

## VAL/SP/checkpoints

See [protocol matrix](wp-002/PROTOCOL_MATRIX.md), [VAL-006 evidence](wp-002/VAL-006.md), [VAL-003/004 evidence](wp-002/VAL-003-004.md), [Android partial closeout](wp-002/raw/VAL003_VAL004_ANDROID_S23_PARTIAL_MINIMIZED.md), and machine-readable [state](wp-002/STATE.json). No canonical `VAL` or `SP` has `PASS`. SP-007 and VAL-001/003/004/006 are operationally `PARTIAL`; VAL-011 is `BLOCKED`; `VAL-016@P0` is `INCONCLUSIVE`; `VAL-025` is `NOT-EXECUTED`.

## Blockers and residual risks

- Physical Android: the human-side S23 execution of the VAL-003/004 Android parcel is accepted as `PARTIAL` at the exact PR #6 head. Formal Attempt 003 still resolves only the preregistered startup/PSS/warm/crash-free parcel for the S23 line. Remaining SP-007/VAL-001 matrix and protocol portions are incomplete, Formal Attempt 004 is prohibited, and this executor still has no ADB channel.
- Physical iOS: blocked by missing iPhone/macOS/Xcode/provisioning.
- Query, scroll, kill/restart, and specific heap budgets remain `NOT-PROPOSED`; the approved S23 startup/PSS budgets cannot be generalized to the Alpha matrix.
- `VAL-003/004`: the Android parcel is no longer pending and is accepted as `PARTIAL`; `rekey-interruption`, `screen-lock-sealed-state`, and `biometric-change` remain inconclusive, Auth A/B isolation was not evaluated, two synthetic accounts/Auth sandbox are absent, and iOS is unavailable. The parcel does not complete VAL-003/004.
- `VAL-006`: Node 24 and controlled local OS-oracle evidence are partial. Hermes Android is authorized and prepared but cannot run without the S23 ADB channel; Hermes iOS and completed cross-runtime matrix remain blocked by the missing Apple environment.
- `VAL-011`: blocked by missing Docker/Podman/PostgreSQL runner plus unprovisioned Supabase/Cloud Run sandbox and cost envelope.
- `VAL-016@P0`: inconclusive while two synthetic accounts/Auth sandbox are absent.
- GitHub Free/private `main` has no remote enforcement; the compensatory IPD-014 process and its residual risk remain active.

No fallback has been activated. Missing or incomplete evidence is not a demonstrated technology failure.

## Closeout result

The WP-002 completion rule is satisfied at the work-package level: every owned protocol/checkpoint has evidence or an explicit partial/blocked/inconclusive disposition, and nothing was promoted by assumption. Human review approved exact head `18a558721772868de1839bec3844aac838528ecf` with successful runs `34766902701` and `34766902705`; PR #4 was integrated through merge commit `35d8edd749fb1e1d77bf95ff18503a88834fd938`. WP-002 is `Done`. G0-A / INC-P0-01 remains not accepted, and WP-003 remains ineligible, because the structural decisions still lack complete VAL-001/003/004/006/011 evidence and a sufficient `VAL-016@P0` checkpoint. The accepted Android VAL-003/004 parcel does not change those gates.
