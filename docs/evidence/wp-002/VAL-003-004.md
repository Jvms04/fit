# VAL-003 / VAL-004 — static readiness and accepted Android partial evidence

- WP: `WP-002`
- Operational state: `PARTIAL`
- Evidence kind: `STATIC-READINESS + ACCEPTED-ANDROID-PARTIAL`
- Canonical promotion: `false`
- Fallback: not activated
- Date: 2026-09-14

## Static readiness

This parcel executes only the locally available readiness checks. It does not treat dependency presence or configuration as proof of encryption, custody, isolation, backup behavior, or lifecycle correctness.

The reproducible inspector verifies the locked disposable harness configuration:

- `expo-sqlite@57.0.2` is configured with `useSQLCipher: true`;
- `expo-secure-store@57.0.3` is used for the probe key;
- `expo-crypto@57.0.2` generates 32 random bytes through `getRandomBytesAsync`;
- key-generation source contains no `Math.random`;
- the probe requests device-only accessibility and disables Android backup in its manifest configuration.

Machine evidence: [`raw/VAL003_VAL004_STATIC_READINESS.json`](raw/VAL003_VAL004_STATIC_READINESS.json), 1170 bytes, SHA-256 `98c64c906b43377dfe6f55a06894e1d80558c76917940e8737b666d03607472d`.

## Accepted Android physical parcel

The human-authorized S23 execution at exact head `5b008e9b6782994359d399f5998c9ffd1bb2234e` is accepted as a valid `PARTIAL` evidence parcel. Its minimized custody record is [`raw/VAL003_VAL004_ANDROID_S23_PARTIAL_MINIMIZED.md`](raw/VAL003_VAL004_ANDROID_S23_PARTIAL_MINIMIZED.md). The raw 24327-byte report remains in human custody with SHA-256 `3B9A722C56F609707BD6B6DB624700E539B047B9320A6900A4CB85C9E3AFCA7A`; the APK SHA-256 is `07412e3891090700f77b1f52a0cd1325c1243a1edda180c731c86240d33a06b6`.

Measured Android operations are:

- VAL-003 parcel: `create-db-wal-shm-canaries`, `extract-db-wal-shm`, `wrong-key`, `rekey`, `restart-recovery`, and `integrity-after-recovery`;
- VAL-004 parcel: `securestore-keystore`, `csprng`, and `restart-process`.

The Android parcel remains limited: `rekey-interruption`, `screen-lock-sealed-state`, and `biometric-change` are `INCONCLUSIVE`. Rekey interruption is `INCONCLUSIVE` because `completionObservedBeforeForceStop=true` and `completionObservedAfterForceStop=true`. Account isolation is `NOT_EVALUATED_WITHOUT_AUTH_A_B`.

VAL-003 therefore remains `PARTIAL`: the accepted Android parcel does not complete interrupted-rekey semantics, the required physical matrix, or any remaining complete protocol/performance criteria. VAL-004 remains `PARTIAL`: Auth A/B isolation, iOS coverage, and the remaining lock/biometry/restore lifecycle evidence are not complete. No canonical promotion is inferred.

Physical iOS remains blocked. No technology failure is demonstrated and no fallback is authorized.
