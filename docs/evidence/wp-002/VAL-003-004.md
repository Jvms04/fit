# VAL-003 / VAL-004 — static readiness evidence

- WP: `WP-002`
- Operational state: `PARTIAL`
- Evidence kind: `STATIC-READINESS-ONLY`
- Canonical promotion: `false`
- Fallback: not activated
- Date: 2026-09-13

This parcel executes only the locally available readiness checks. It does not treat dependency presence or configuration as proof of encryption, custody, isolation, backup behavior, or lifecycle correctness.

The reproducible inspector verifies the locked disposable harness configuration:

- `expo-sqlite@57.0.2` is configured with `useSQLCipher: true`;
- `expo-secure-store@57.0.3` is used for the probe key;
- `expo-crypto@57.0.2` generates 32 random bytes through `getRandomBytesAsync`;
- key-generation source contains no `Math.random`;
- the probe requests device-only accessibility and disables Android backup in its manifest configuration.

Machine evidence: [`raw/VAL003_VAL004_STATIC_READINESS.json`](raw/VAL003_VAL004_STATIC_READINESS.json), 1170 bytes, SHA-256 `98c64c906b43377dfe6f55a06894e1d80558c76917940e8737b666d03607472d`.

VAL-003 still lacks DB/WAL/SHM extraction with canaries, wrong-key rejection, rekey interrupted by crash, intact recovery, and a preregistered performance criterion on the required physical matrix. SQLCipher `4.7.0 community` observed in the accepted S23 characterization proves runtime presence only.

VAL-004 still lacks login A → lock/logout → switch B → restart → changed biometry → restore on physical devices with two synthetic accounts and an Auth sandbox. The static checks do not prove that A cannot be opened by B, sealed-state behavior, preservation of pending operations, or backup non-reopen.

Physical iOS remains blocked. No technology failure is demonstrated and no fallback is authorized.
