# VAL-006 — Node 24 partial evidence

- WP: `WP-002`
- Protocol state: `PARTIAL`
- Canonical promotion: `false`
- Runtime executed: Node `v24.19.0`
- Hermes Android: `NOT-EXECUTED`
- Hermes iOS: `BLOCKED`
- Fallback: not activated
- Date: 2026-09-12

This record covers only the explicitly authorized Node 24 parcel of `VAL-006`. It is not an integral result for the canonical validation and does not replace the required Hermes Android/iOS and cross-runtime evidence.

## Frozen stack and immutable rule-base

| Item | Observed value |
|---|---|
| `@js-temporal/polyfill` | `0.5.1` |
| `moment-timezone` | `0.6.3` |
| IANA TZDB | `2026c` |
| Full packed bundle size | `715527` bytes |
| Full packed bundle SHA-256 | `43f7878a298740ff6acabb9c726c7e5431a94bdca79abad274a6fe6e355bfe81` |
| `rule_base_id` | `iana-2026c+moment-timezone-0.6.3+sha256:43f7878a298740ff6acabb9c726c7e5431a94bdca79abad274a6fe6e355bfe81` |

The bundle hash is calculated from the exact bytes installed by the locked root `package-lock.json`. The OS TZDB is not used as the rule authority by this probe.

## Executed protocol

The disposable Node harness executed five literal vectors under the same bundle and hash:

| Vector | Frozen semantic | Node 24 result |
|---|---|---|
| New York DST gap | advance to the next valid local instant | matched |
| New York DST overlap | first chronological occurrence | matched |
| Lord Howe 30-minute gap | advance to the next valid local instant | matched |
| Lord Howe 30-minute overlap | first chronological occurrence | matched |
| Kiritimati year/date rollover | preserve exact instant/offset conversion | matched |

Result: `5/5` vectors matched. A second offline execution produced the same serialized vector result; the harness requires no network.

The handshake probe classified an identical `rule_base_id` as `MATCH` and a different hash as `RULE_BASE_SKEW`. A protocol-only future-rule fixture was preregistered by its exact bytes:

- asset: `tests/temporal/wp-002/fixtures/synthetic-future-revision.json`;
- size: `231` bytes;
- SHA-256: `9f2e71444ea8d2541702843c386032e62f8a5b9f10360440fd9fdfe2a779feaa`;
- derived synthetic rule-base ID: `iana-synthetic-future+sha256:9f2e71444ea8d2541702843c386032e62f8a5b9f10360440fd9fdfe2a779feaa`.

The fixture changed only the future unconsolidated protocol record. The consolidated record preserved its old instant, offset, identity, version, and rule-base ID; the past record also remained unchanged. These records are synthetic protocol vectors, not Product/domain implementation.

## Reproducible evidence

- Command: `npm run probe:val006:node24`
- Machine report: [`raw/VAL006_NODE24_PARTIAL.json`](raw/VAL006_NODE24_PARTIAL.json)
- Report size: `6699` bytes
- Report SHA-256: `571cc728af288dcc6e220f5f3ff793a70bdee799a7ed0972e9a31ce796dd4e86`
- Tests: temporal rule-base, skew/reconciliation, CLI/runtime guard, and WP-002 state guardrails

## Remaining before canonical VAL-006

- execute the identical versioned corpus under Hermes Android;
- execute the identical versioned corpus under Hermes iOS (currently blocked by absent iPhone/macOS/Xcode/provisioning);
- compare cross-runtime outputs under the same `rule_base_id`;
- exercise the deliberately divergent OS-TZDB condition required by the integral protocol;
- complete any remaining canonical corpus coverage owned by the later temporal implementation without promoting this disposable harness to production.

No stack failure was demonstrated. The frozen FormatJS fallback remains inactive and cannot be activated by this partial parcel.
