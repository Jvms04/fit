# SP-007 — Initial characterization evidence

- Stage: `INITIAL-CHARACTERIZATION`
- Canonical SP status: `Defined with validation pending`
- WP-002 state: `IN-PROGRESS`
- Initial-characterization budget applied: no
- S23 formal-run budgets: `APPROVED-PREREGISTERED`
- Formal validation: no

## Authorized protocol

The initial run uses the disposable Expo SDK 57 / React Native 0.86.3 release harness, five cold starts, five warm starts, Android `am start -W`, `dumpsys meminfo`, and harness markers for SQLite/SQLCipher and synthetic database preparation. The exploratory fixture contains 1,000 deterministic rows and is explicitly not an approved Alpha volume. Warm preparation exits the activity with `KEYCODE_BACK`; the sample is accepted only when Android reports `LaunchState: WARM` and a finite non-negative `TotalTime`.

The runner refuses 30 samples so this stage cannot silently become `VAL-001`. Raw samples must be retained before summaries. `UNKNOWN (0)` is `NOT_A_LAUNCH_EVENT`; an unexpected state or absent `TotalTime` makes the attempt diagnostic-only. `WaitTime` is preserved raw and never substituted for `TotalTime`. Any budget proposal is reviewed and preregistered by a human before a formal run.

## Environment and evidence

Human-confirmed device: Samsung Galaxy S23 (SM-S911B) / Android 16, API 36. The executor installed official Android Platform-Tools 37.0.1 and observed no USB or wireless ADB target, so physical execution used the human-side Windows ADB channel.

Attempt 001 remains an [aborted diagnostic](raw/SP007_S23_ATTEMPT_001_DIAGNOSTIC.md). Its zero-byte result is invalid and contributes no characterization measurement.

Corrected attempt 002 used the release APK built for harness head `52ba378e434e2d13fe1f07564ce730850391a708` by workflow run `34488068572` (artifact `10157046732`). It exited zero and produced an `INITIAL_CHARACTERIZATION_CANDIDATE` eligible for the partial S23 row:

| Measurement | Raw result |
|---|---:|
| Cold validity | 5/5 `COLD / MEASURED` |
| Warm validity | 5/5 `WARM / MEASURED` |
| Cold `TotalTime` | min 174 ms; median 286 ms; p95/max 365 ms |
| Warm `TotalTime` | min 85 ms; median 97 ms; p95/max 125 ms |
| Cold `TOTAL PSS` | 104027–118305 KB |
| Probe versions | SQLite 3.49.1; SQLCipher 4.7.0 community |
| Dataset | 1000 deterministic synthetic rows |

The exact source is retained as [raw UTF-16LE](raw/SP007_S23_ATTEMPT_002_RAW_UTF16LE.json), with byte/hash and transformation metadata in the [provenance record](raw/SP007_S23_ATTEMPT_002_PROVENANCE.json). The [canonical UTF-8 representation](raw/SP007_S23_ATTEMPT_002_CANONICAL_UTF8.json) is parsed-JSON equivalent and uses two-space indentation, LF formatting, one final newline, and no BOM. JSON string values are unchanged; no Unicode normalization is applied.

Android CLI and Gradle were also prepared locally. JVM downloads required by SDK/Gradle dependency resolution are unavailable in this executor, so the same build is delegated to the repository's ephemeral GitHub Actions job. This is an environment limitation, not a framework result.

iOS remains `BLOCKED`: no physical iPhone, macOS, Xcode, or provisioning exists. No simulator substitutes for this missing evidence.

## Approved provisional S23 budgets

The human-approved [budget record](SP007_S23_BUDGETS.json) is preregistered for future executions that begin after the repository commit containing that record. It is never applied retrospectively to attempt 002, whose immutable report says `budgetApplied: false` and `formalValidation: false`.

| Metric | Approved provisional budget |
|---|---:|
| Cold startup `TotalTime`, p95 | <= 600 ms |
| Warm startup `TotalTime`, p95 | <= 200 ms |
| Cold `TOTAL PSS`, p95 | <= 133120 KB (130 MiB) |
| Valid samples | 30/30 `COLD / MEASURED`; 30/30 `WARM / MEASURED` |
| Crashes | 0 |

The guardband uses the observed maximum plus one complete observed range, rounded upward: cold `365 + 191 = 556 -> 600 ms`; warm `125 + 40 = 165 -> 200 ms`; PSS `118305 + 14278 = 132583 -> 133120 KB`. This is an operational proposal derived from the five-sample envelope, not a robust p95 estimate.

These budgets apply only to the Samsung Galaxy S23 / Android 16 release harness with the 1000-row synthetic dataset. They are not Alpha-wide, establish no Android minimum/intermediate device, and cover no iOS line. Query, scroll, kill/restart, and specific heap budgets remain `NOT-PROPOSED`. `readyMs` is retained only as probe diagnostics and is never substituted for Android startup `TotalTime`.

For 30 samples, nearest-rank p95 is the 29th ordered observation. All raw observations plus minimum, median, p95, maximum, hardware/OS/build, and crashes must be retained. An unexpected launch state or missing `TotalTime` invalidates the requested sample rather than becoming zero or `WaitTime`.

## Formal-run executable preparation

The distinct formal runner is `tests/performance/wp-002/formal-android.mjs`. It does not weaken or add a mode to the initial-characterization runner: the latter continues to reject sample counts outside 3–10, while the formal runner accepts no sample-count argument and always requests exactly 30 cold plus 30 warm launches.

Before any ADB command, the formal runner requires an explicit preregistration commit, proves that it is an ancestor of the checked-out source head, requires the source head to be later, and rejects any change to `SP007_S23_BUDGETS.json` across that range. It reads the budgets from the preregistration commit itself and validates their approved scope and protocol fields. The approved preregistration commit is `4f108a56d1b3553836eac65662d449809715ee87`; the budget file remains SHA-256 `ffc27cd2de0de43180f11d02fac07fbae2eb5ed3ec3180d1d873a933a90de0f6`.

The CI artifact contains `app-release.apk`, `APK_PROVENANCE.json`, and `APK_PROVENANCE.sha256`. Provenance records the exact source head/run/job, release build contract, application ID, byte size, and APK SHA-256 derived from the produced APK and its analyzed manifest. Before the first sample, the runner matches repository head to that record, matches the downloaded APK bytes, pulls the installed `base.apk` from the explicitly selected device and matches its bytes, then verifies the package-scoped runtime marker and 1000-row synthetic dataset. A missing link aborts before sampling.

Crash evidence uses the probe package/process family from `dumpsys activity exit-info`, with before/after raw history retained. Runner-issued reason 10 `force-stop` is expected; Java/Kotlin crash, native crash, ANR, signals, initialization failure, and other non-protocol exits are abnormal. ADB interruption after acquisition begins still emits partial `ABORTED_DIAGNOSTIC` JSON and a nonzero exit code.

Every launch retains the raw `am start -W` output and its protocol classification. Cold samples also retain raw `dumpsys meminfo`. Only `TotalTime` is summarized for startup; `WaitTime` and probe `readyMs` cannot substitute. The implementation uses nearest-rank p95 through rank `ceil(30 * 0.95) = 29`, preserves minimum/median/p95/maximum, checks cold `TOTAL PSS`, and evaluates package/process-scoped Android `ApplicationExitInfo` history captured before and after acquisition. An incomplete or invalid measurement set is `ABORTED_DIAGNOSTIC`; a valid set remains a `FORMAL_RUN_EVIDENCE_CANDIDATE` even if it exceeds a budget. Either outcome requires independent human review and performs no canonical promotion.

Preparation is `PREPARED-NOT-EXECUTED`. No physical 30+30 run has started. Head `2ddd535bcdb95ac7259f1ce2ab16098255446ee6` was rejected by independent audit before physical use. Its corrective successor requires green Ubuntu and Windows CI, the exact release APK/provenance artifact, and a new human authorization before human-side execution.

## Current result

The Galaxy S23 initial characterization is valid partial SP-007 evidence. The formal 30+30 executable is prepared but has not been run. Neither condition completes the device matrix nor starts or passes VAL-001. SQLCipher version presence does not demonstrate encrypted files, wrong-key behavior, rekey, crash recovery, or performance and therefore does not promote VAL-003. No fallback is active.

Protocol semantics follow the frozen cold/warm requirement and Android's definitions: [Android app startup states](https://developer.android.com/topic/performance/vitals/launch-time) and AOSP [`WaitResult`](https://github.com/aosp-mirror/platform_frameworks_base/blob/master/core/java/android/app/WaitResult.java).
