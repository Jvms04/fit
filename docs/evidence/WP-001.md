# WP-001 — Operational evidence package

## Gate proposal

| Field | Evidence |
|---|---|
| WP / increment | `WP-001` — G0 / preparation for `INC-P0-01` |
| Operational state | `In Review` |
| Date | 2026-09-08 UTC |
| Repository | private `Jvms04/fit` |
| Branch | `wp-001-governance` |
| Local implementation head | `fc4bd54e03082188dd2dfe7ff9094a5f16dbdb7c` |
| Remote implementation snapshot | `d5af131090cfcea6aa40a68c117254e6091854c2` |
| Pull request | [PR #1 — WP-001: governança, boundaries e evidence index](https://github.com/Jvms04/fit/pull/1), draft |
| Owner / CODEOWNER | `@Jvms04` |
| Human gate | Pending review; this document does not accept the WP or increment |

The remote snapshot is the implementation anchor immediately before this evidence document. PR #1 remains the stable review surface as evidence is added.

## Authority and scope

- Product → Architecture → Technical Architecture → Stack → Implementation Plan.
- `IMPLEMENTATION_WORK_BREAKDOWN.md` §5, row `WP-001`: repository guardrails, ownership, evidence index, static boundary/schema-drift checks, and `VAL-025` preparation.
- `PHASE7_EXECUTION_BRIEF.md` §1: G0 begins with `WP-001`/`WP-002`, and evidence precedes promotion.
- `VALIDATION_AND_SPIKE_EXECUTION_PLAN.md`, `VAL-025`: supply-chain/signing/delivery protocol remains pending until its full environment and protocol exist.
- `PHASE6_DECISION_REGISTER.md`: `IPD-009`, `IPD-010`, `IPD-014`, `IPD-015`, `IPD-021`, and `IPD-023` govern the logical monorepo, absence of generic `shared`, short WP branch, progressive CI, isolated execution, and explicit human gates.

Delivered scope is limited to governance documents, ownership, exact baseline hash metadata, execution/evidence templates, future-root policies, executable repository/boundary/schema/manifest checks, Node tests, and a read-only GitHub Actions workflow.

## Baseline custody

The five approved source ZIPs were read from the external scratch handoff and were not copied into the repository. Their verified SHA-256 values and canonical counts are recorded in [the baseline manifest](../baselines/MANIFEST.md). Candidate, Patched, Draft, and intermediate artifacts were not used as authority.

## Tracked-file inventory

The remote implementation snapshot contains exactly these 22 tracked paths:

```text
.github/CODEOWNERS
.github/workflows/governance.yml
.gitignore
README.md
docs/baselines/MANIFEST.md
docs/evidence/INDEX.md
docs/governance/BOUNDARIES.md
docs/governance/CODEX_EXECUTION_PACKET_TEMPLATE.md
docs/governance/DECISION_TEMPLATE.md
docs/governance/VAL_EVIDENCE_TEMPLATE.md
package-lock.json
package.json
tooling/.gitkeep
tooling/check-baseline-manifest.mjs
tooling/check-boundaries.mjs
tooling/check-repository.mjs
tooling/check-schema-drift.mjs
tooling/governance-utils.mjs
tooling/policies/boundaries.json
tooling/policies/schema-source.json
tooling/run-governance-checks.mjs
tooling/tests/governance.test.mjs
```

## Local verification

| Command | Observed result |
|---|---|
| `node --version` | `v24.19.0`; exit 0 |
| `git --version` | `git version 2.51.1`; exit 0 |
| `npm run verify:governance` | repository, boundaries, schema, and baseline-manifest checks all completed successfully; exit 0 |
| `npm test` | 27 tests, 27 passed, 0 failed; exit 0 |
| `npm pack --dry-run --json` | 21 package entries in the evidence tree (20 in the pre-evidence implementation snapshot); no production scaffold, baseline ZIP, secret, database, or `.superpowers` artifact; exit 0 |
| `git diff --check` | no whitespace error; exit 0 |

The package inventory has fewer entries than the Git inventory because npm intentionally excludes metadata such as `.gitignore` and `package-lock.json`. It was not published. Npm emitted non-failing environment and `.npmignore` fallback warnings; neither changed the inventory or initiated registry access.

The negative controls exercise premature future roots, case-insensitive generic `shared`, symlinks, forbidden domain/application imports, relative and aliased infrastructure imports, parser edge cases, Unicode offsets, schema source drift, generated-schema provenance/hash, secret/environment/key names, altered baseline metadata, and an extra manifest row.

## Remote verification

GitHub Actions run `34263531913` (`Governance`, run 2) completed successfully for remote snapshot `d5af131090cfcea6aa40a68c117254e6091854c2`. Job `102187120715` completed successfully on `ubuntu-24.04`; its successful steps were:

1. `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1` with persisted credentials disabled;
2. `actions/setup-node@94196ee1d15439c1b6651cd87ef14e88ec435966` with Node 24.19.0 and no package-manager cache configuration;
3. `npm run verify:governance`;
4. `npm test`;
5. `npm pack --dry-run`.

The workflow has `contents: read`, no write permission, no secret reference, and no publish step.

## Reviews and corrections

- Governance documentation received central review; database responsibilities were separated and omitted frozen constraints were restored before acceptance.
- Executable checks received adversarial review. Three bounded correction rounds closed schema naming/comment/regex cases, concrete infrastructure imports, contextual import parsing, extra manifest rows, signing extensions, and UTF-16 comment-mask offsets.
- The final directed re-review found no open Critical or Major item in Task 2.

## Prohibitions and absence evidence

- No production scaffold or application workspace declaration.
- No `apps/`, `services/`, `packages/`, `database/`, `infra/`, or root `tests/` area was created.
- No domain model, database, migration, UI, Figma artifact, real/synthetic account data, or cloud configuration was created.
- No generic `shared` directory/package, secret, signing key, certificate, `.env`, or local database artifact is tracked.
- No dependency was added; the lockfile has an empty dependency graph.
- No baseline source or ZIP is tracked or modified.
- No `WP-002` probe was executed and no `WP-003` work was started.

## VAL/checkpoint governance

| Item | Operational record |
|---|---|
| `VAL-025` preparation | `Prepared`: workflow skeleton, lockfile, action SHA pins, permissions, package inventory, and evidence template exist |
| Canonical `VAL-025` outcome | Unchanged; full two-build/SBOM/signing/install/update/rollback protocol not executed |
| Evidence checkpoints | None promoted; no checkpoint disposition assigned by this WP |
| WP/increment acceptance | Not assigned; requires human review |

`Prepared` is an operational readiness label, not a canonical VAL outcome and not a checkpoint disposition.

## WP-002 blockers preserved

WP-002 remains `Blocked`. The following probe inputs are still required or must be confirmed:

- Android SDK/ADB and an Android physical device;
- macOS/Xcode/provisioning and an iPhone physical device;
- Docker/Testcontainers or an equivalent runner;
- Supabase sandbox/Pro in São Paulo;
- GCP/Cloud Run `southamerica-east1` with billing;
- two synthetic accounts;
- effective access to the dependency registry.

Apple Developer/TestFlight, Google Play signing, EAS, and P1 scientific dependencies should be requested early but do not block this WP-001 gate. No `VAL` result can be inferred from those pending resources.

## Human review request

Review PR #1 against the `WP-001` row and this evidence package. The proposed operational transition is `In Progress` → `In Review`; `Done` and increment acceptance remain unavailable until the human gate is recorded.
